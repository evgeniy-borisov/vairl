# Двуязычная версия сайта VAIRL

## ✅ Реализовано

Сайт теперь поддерживает **2 языка**:
- 🇬🇧 **Английский (EN)** - по умолчанию
- 🇷🇺 **Русский (RU)**

## 🎯 Функционал

### Переключатель языка
- Кнопки EN/RU в правом верхнем углу
- Активная кнопка подсвечена синим
- Плавное переключение без перезагрузки страницы
- Выбранный язык сохраняется в localStorage

### Что переводится:
- ✅ Подзаголовок
- ✅ Описание лаборатории
- ✅ Призыв к действию
- ✅ Навигация (home/blog)
- ✅ Все ссылки

## 📂 Структура файлов

### Новые файлы:
```
_data/
  └── translations.yml          # Переводы для Jekyll

assets/js/
  └── language-switcher.js      # JavaScript для переключения языка
```

### Изменённые файлы:
```
_config.yml                     # Добавлены настройки языков
_layouts/default.html          # Добавлен переключатель
assets/css/style.css           # Стили для переключателя
index.html                     # Добавлены атрибуты data-i18n
preview.html                   # Обновлённая версия для просмотра
```

## 🔧 Как это работает

### 1. HTML разметка
Каждый переводимый элемент имеет атрибут `data-i18n`:

```html
<p data-i18n="tagline">Virtual AI Research Lab</p>
```

### 2. JavaScript
При клике на кнопку языка:
1. Находит все элементы с `data-i18n`
2. Заменяет текст на перевод из словаря
3. Сохраняет выбор в localStorage
4. Обновляет активное состояние кнопок

### 3. Переводы
Все переводы хранятся в `assets/js/language-switcher.js`:

```javascript
const translations = {
    en: { tagline: "Virtual AI Research Lab", ... },
    ru: { tagline: "Виртуальная лаборатория...", ... }
};
```

## 📱 Адаптивность

Переключатель языка адаптируется под размер экрана:
- Desktop: в правом верхнем углу
- Mobile: также сверху справа, не ломает layout

## 🎨 Стили

```css
.lang-switch           /* Обычное состояние */
.lang-switch:hover     /* При наведении */
.lang-switch.active    /* Активный язык */
```

## ➕ Добавление нового языка

Для добавления языка (например, японского):

1. **Добавить переводы** в `language-switcher.js`:
```javascript
ja: {
    tagline: "バーチャルAI研究室",
    // ...
}
```

2. **Добавить кнопку** в layout:
```html
<button class="lang-switch" data-lang="ja" 
        onclick="switchLanguage('ja')">JA</button>
```

3. **Готово!** Всё остальное работает автоматически.

## 💾 Сохранение выбора

Выбранный язык сохраняется в `localStorage` браузера:
- При первом посещении: английский (EN)
- При повторном посещении: последний выбранный язык

## 🚀 Использование

### В браузере:
1. Откройте сайт
2. Нажмите EN или RU
3. Весь текст мгновенно переключится
4. Язык запомнится для следующего визита

### Для Jekyll:
Можно также использовать `_data/translations.yml` для серверного рендеринга с Jekyll, но текущая реализация работает на клиенте для простоты и скорости.

---

Сайт готов к публикации с поддержкой двух языков! 🎉

